#include <iostream>
using namespace std;

int main()
{
	int n;
	cout << "enter n (n>0, if n <=0 consol out sum=0): ";
	cin >> n;
	float sum = 0;
	for (float i = 1; i <= n; ++i) {

			sum += (1 / (((2 * i) + 1) * ((2 * i) + 1)));
		
    }
	cout << "sum= " << sum;
}

